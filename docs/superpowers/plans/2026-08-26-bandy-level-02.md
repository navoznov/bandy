# Второй уровень Bandy — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в игру второй уровень — подвал с цепочкой из двух ключей — и
переход на него с экрана победы первого.

**Architecture:** Уровень целиком описывается данными: `src/levels/level_02.json`
плюс три определения предмета в общем `src/levels/items.json`. Порядок уровней
живёт в реестре внутри `src/levels/index.ts`; `main.ts` берёт идентификатор из
`location.hash` и после победы перезагружает страницу с новым хешем, переиспользуя
весь существующий путь загрузки. Ядро и рендер не трогаются вовсе.

**Tech Stack:** TypeScript (strict, `noUncheckedIndexedAccess`), Vitest, Vite,
Three.js (в этой задаче не затрагивается).

**Spec:** `docs/superpowers/specs/2026-08-26-bandy-level-02-design.md` — читать
целиком до первой задачи. Базовая спека проекта —
`docs/superpowers/specs/2026-08-24-bandy-design.md`.

## Global Constraints

- **`src/core/` не импортирует three.js. Никогда.** (CLAUDE.md)
- **Задачи 1-3 не имеют права править `src/core/interactions.ts`,
  `src/core/types.ts`, `src/core/validate.ts` и что-либо в `src/render/`.** Весь
  смысл работы — выяснить, собирается ли уровень без правок ядра. Если задача не
  решается без такой правки — **остановиться и доложить**, а не чинить молча. Это
  находка, а не препятствие.
- Единицы — метры, сетка 1 м. Проём двери 0.9 × 2.1, стены 0.2, комнаты высотой 3.0.
- Предметы кладутся на пол: `y = 0.02`. Высота 0.9 уже была живым дефектом.
- **Финальный коридор идёт вдоль +z, белым торцом в сторону больших z.** Это
  требование `buildExitGlow()` в `src/render/sign.ts`, и формат о нём не сообщает.
- Камера смотрит в `(-sin yaw, -cos yaw)`; `yaw = -1.5708` — взгляд вдоль +x.
- Хоткеи читаются только через `event.code`.
- `npm test` (это `tsc --noEmit && vitest run`) обязан быть зелёным в конце каждой
  задачи. Сейчас тестов 102.
- Сообщения коммитов на английском, повелительное наклонение, без строки
  `Co-Authored-By` и без пометок про генерацию.

---

## Файлы

| Файл | Что с ним | Ответственность |
|---|---|---|
| `src/levels/level_02.json` | создать | Данные второго уровня целиком |
| `src/levels/items.json` | дополнить | Общий на игру реестр определений предметов |
| `src/levels/level_02.test.ts` | создать | Валидность данных уровня и негативная проверка достижимости |
| `src/levels/index.ts` | переписать | Реестр уровней, `loadLevel(id?)`, `nextLevelId()` |
| `src/levels/registry.test.ts` | создать | Поведение реестра |
| `src/core/walkthrough-02.test.ts` | создать | Сквозной проход второго уровня |
| `index.html` | дополнить | Кнопка «Дальше» на экране победы и её стили |
| `src/main.ts` | точечно править | Чтение хеша при загрузке, показ кнопки при победе |
| `known-issues.md` | дополнить | Запись про зашитую ориентацию финального коридора |
| `CLAUDE.md` | дополнить | Два уровня, реестр, переход |

---

### Task 1: Данные второго уровня

**Files:**
- Create: `src/levels/level_02.json`
- Modify: `src/levels/items.json`
- Test: `src/levels/level_02.test.ts`

**Interfaces:**
- Consumes: `validateLevel(raw: unknown, itemDefs: Record<string, ItemDef>)` из
  `src/core/validate.ts` — возвращает `{ ok: true; level: Level }` либо
  `{ ok: false; errors: string[] }`.
- Produces: файл `src/levels/level_02.json` с идентификатором уровня `level_02` и
  определения предметов `key_iron`, `key_steel`, `pipe` в `src/levels/items.json`.
  Задача 2 подключает файл к реестру, задача 3 проходит уровень насквозь.

- [ ] **Step 1: Написать падающий тест**

Создать `src/levels/level_02.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import rawLevel from './level_02.json';
import rawItems from './items.json';
import { validateLevel } from '../core/validate';
import type { ItemDef } from '../core/types';

const defs = rawItems as unknown as Record<string, ItemDef>;

describe('level_02', () => {
  it('проходит валидацию', () => {
    const result = validateLevel(rawLevel, defs);
    // Ошибки печатаются целиком: список из валидатора и есть диагноз.
    expect(result.ok ? [] : result.errors).toEqual([]);
  });

  it('в уровне десять комнат, два замка и три предмета', () => {
    const result = validateLevel(rawLevel, defs);
    if (!result.ok) throw new Error(result.errors.join('\n'));
    expect(result.level.rooms).toHaveLength(10);
    expect(result.level.doors.filter((d) => d.lock !== undefined)).toHaveLength(2);
    expect(result.level.items).toHaveLength(3);
  });

  /**
   * Главная проверка данных этого уровня. Цепочка «ключ за дверью, которую он же
   * открывает» — единственная ошибка автора карты, которую невозможно заметить
   * глазами в редакторе и очень легко сделать. До сих пор проверка достижимости
   * нагружалась только синтетическими уровнями из validate.test.ts.
   */
  it('железный ключ, положенный за собственную решётку, делает уровень непроходимым', () => {
    const broken = structuredClone(rawLevel);
    const iron = broken.items.find((it) => it.def === 'key_iron');
    if (!iron) throw new Error('В уровне нет key_iron — тест устарел вместе с картой.');
    iron.room = 'elec';
    iron.at = [9, 0.02, 4];

    const result = validateLevel(broken, defs);
    expect(result.ok).toBe(false);
    if (!result.ok) {
      // Валидатор выдаёт здесь две ошибки, и требовать надо точную: она называет
      // ключ, замок и комнату, то есть чинится по ней без раздумий. Общая
      // «уровень непроходим» — следствие, и одной её мало.
      expect(result.errors).toContain(
        'Ключ "Железный ключ" нужен для замка "lock_grate", но лежит в комнате "elec", '
        + 'куда нельзя попасть, не открыв этот замок.',
      );
      expect(result.errors).toContain(
        'Уровень непроходим: победа недостижима из точки появления.',
      );
    }
  });
});
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `npm test src/levels/level_02.test.ts`
Expected: FAIL. Модуль `./level_02.json` не найден — файла ещё нет.

- [ ] **Step 3: Дополнить реестр предметов**

Заменить `src/levels/items.json` целиком:

```json
{
  "key_brass": { "id": "key_brass", "name": "Латунный ключ", "holdable": true },
  "key_iron":  { "id": "key_iron",  "name": "Железный ключ", "holdable": true },
  "key_steel": { "id": "key_steel", "name": "Стальной ключ", "holdable": true },
  "pipe":      { "id": "pipe",      "name": "Обрезок трубы", "holdable": true }
}
```

- [ ] **Step 4: Написать данные уровня**

Создать `src/levels/level_02.json`. Координаты выверены числами по правилу
`checkSharedWall` из валидатора — менять их без пересчёта нельзя:

```json
{
  "id": "level_02",
  "spawn": { "room": "boiler", "x": 2, "z": 8, "yaw": -1.5708 },

  "rooms": [
    { "id": "boiler",     "rect": [0, 6, 6, 4],   "color": "#4a4640", "light": 0.55 },
    { "id": "corr_main",  "rect": [6, 7, 15, 2],  "color": "#3f4245", "light": 0.45 },
    { "id": "elec",       "rect": [6, 2, 6, 5],   "color": "#57503f", "light": 0.7 },
    { "id": "dead_pit",   "rect": [12, 2, 6, 5],  "color": "#33363a", "light": 0.35 },
    { "id": "store",      "rect": [6, 9, 6, 5],   "color": "#4e4a44", "light": 0.6 },
    { "id": "pump",       "rect": [12, 9, 6, 5],  "color": "#3a4a4a", "light": 0.4 },
    { "id": "corr_north", "rect": [6, 14, 12, 2], "color": "#3f4245", "light": 0.45 },
    { "id": "archive",    "rect": [6, 16, 6, 4],  "color": "#584c46", "light": 0.65 },
    { "id": "cold",       "rect": [12, 16, 6, 4], "color": "#40484f", "light": 0.5 },
    { "id": "exit_hall",  "rect": [18, 9, 3, 16], "color": "#5a5a60", "light": 0.4 }
  ],

  "doors": [
    { "id": "d_boiler_corr",   "between": ["boiler", "corr_main"],    "at": [6, 8] },
    { "id": "d_corr_store",    "between": ["corr_main", "store"],     "at": [9, 9] },
    { "id": "d_corr_pump",     "between": ["corr_main", "pump"],      "at": [15, 9] },
    { "id": "d_corr_elec",     "between": ["corr_main", "elec"],      "at": [9, 7],
      "lock": "lock_grate" },
    { "id": "d_corr_pit",      "between": ["corr_main", "dead_pit"],  "at": [15, 7] },
    { "id": "d_store_north",   "between": ["store", "corr_north"],    "at": [9, 14] },
    { "id": "d_north_archive", "between": ["corr_north", "archive"],  "at": [9, 16] },
    { "id": "d_north_cold",    "between": ["corr_north", "cold"],     "at": [15, 16] },
    { "id": "d_corr_exit",     "between": ["corr_main", "exit_hall"], "at": [19.5, 9],
      "lock": "lock_exit", "sign": "EXIT" }
  ],

  "items": [
    { "def": "key_iron",  "room": "archive", "at": [9, 0.02, 18] },
    { "def": "key_steel", "room": "elec",    "at": [9, 0.02, 4.5] },
    { "def": "pipe",      "room": "cold",    "at": [15, 0.02, 18] }
  ],

  "triggers": [
    { "id": "win", "room": "exit_hall", "rect": [18, 22, 3, 3], "effect": "win" }
  ],

  "interactions": [
    { "use": "key_iron", "on": "lock_grate",
      "effects": [
        { "destroy": "lock_grate" },
        { "consume": "key_iron" },
        { "say": "Решётка звякнула и упала на бетон." }
      ] },
    { "use": "key_steel", "on": "lock_exit",
      "effects": [
        { "destroy": "lock_exit" },
        { "consume": "key_steel" },
        { "say": "Замок поддался. Дверь свободна." }
      ] }
  ]
}
```

- [ ] **Step 5: Запустить тест и убедиться, что проходит**

Run: `npm test src/levels/level_02.test.ts`
Expected: PASS, три теста.

Если валидатор ругается — **не подгонять координаты наугад.** Сообщение
валидатора называет и сущность, и допустимый диапазон; сверить с таблицами в
разделе 4 спеки. Если ошибка не про координаты, а про то, чего формат не умеет —
остановиться и доложить.

- [ ] **Step 6: Прогнать весь набор**

Run: `npm test`
Expected: PASS, 105 тестов.

- [ ] **Step 7: Коммит**

```bash
git add src/levels/level_02.json src/levels/items.json src/levels/level_02.test.ts
git commit -m "Add the basement level: two keys, one chain, three dead ends"
```

---

### Task 2: Реестр уровней

**Files:**
- Modify: `src/levels/index.ts` (переписывается целиком, файл на 8 строк)
- Test: `src/levels/registry.test.ts`

**Interfaces:**
- Consumes: `src/levels/level_02.json` из задачи 1.
- Produces:
  - `loadLevel(id?: string): LoadResult` — где
    `LoadResult = { ok: true; level: Level } | { ok: false; errors: string[] }`.
    Без аргумента и при неизвестном `id` грузится первый уровень списка.
  - `nextLevelId(current: string): string | null` — следующий по порядку или `null`.
  - Экспортируемый тип `LoadResult`.

  Задача 4 зовёт обе функции из `src/main.ts`.

**Важно:** реестр не читает `location`. Хеш разбирает `main.ts` и передаёт готовый
идентификатор. Иначе модуль перестанет запускаться в тестах, где нет `window`.

- [ ] **Step 1: Написать падающий тест**

Создать `src/levels/registry.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { loadLevel, nextLevelId } from './index';

describe('реестр уровней', () => {
  it('без аргумента грузит первый уровень', () => {
    const loaded = loadLevel();
    expect(loaded.ok).toBe(true);
    if (loaded.ok) expect(loaded.level.id).toBe('level_01');
  });

  it('грузит уровень по идентификатору', () => {
    const loaded = loadLevel('level_02');
    expect(loaded.ok).toBe(true);
    if (loaded.ok) expect(loaded.level.id).toBe('level_02');
  });

  /**
   * Неизвестный идентификатор — не ошибка уровня, а чужая ссылка в руках игрока.
   * Правильная реакция — начать игру, а не показать экран ошибки.
   */
  it('неизвестный идентификатор молча даёт первый уровень', () => {
    const loaded = loadLevel('level_99');
    expect(loaded.ok).toBe(true);
    if (loaded.ok) expect(loaded.level.id).toBe('level_01');
  });

  it('знает порядок прохождения и конец игры', () => {
    expect(nextLevelId('level_01')).toBe('level_02');
    expect(nextLevelId('level_02')).toBe(null);
    expect(nextLevelId('level_99')).toBe(null);
  });
});
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `npm test src/levels/registry.test.ts`
Expected: FAIL. `nextLevelId` не экспортируется, `loadLevel` не принимает аргумент.

- [ ] **Step 3: Переписать реестр**

Заменить `src/levels/index.ts` целиком:

```ts
import rawLevel01 from './level_01.json';
import rawLevel02 from './level_02.json';
import rawItems from './items.json';
import { validateLevel } from '../core/validate';
import type { ItemDef, Level } from '../core/types';

export type LoadResult = { ok: true; level: Level } | { ok: false; errors: string[] };

/**
 * Порядок прохождения — свойство игры, а не уровня. Поле `next` внутри JSON
 * проверить невозможно: валидатор видит один уровень и о существовании
 * следующего не знает, то есть опечатка всплыла бы у игрока, а не у автора карты.
 */
const LEVELS: ReadonlyArray<{ id: string; raw: unknown }> = [
  { id: 'level_01', raw: rawLevel01 },
  { id: 'level_02', raw: rawLevel02 },
];

const ITEM_DEFS = rawItems as unknown as Record<string, ItemDef>;

/** Неизвестный или отсутствующий идентификатор даёт первый уровень: чужая ссылка
 *  не повод показывать игроку экран ошибки. */
export function loadLevel(id?: string): LoadResult {
  const entry = LEVELS.find((level) => level.id === id) ?? LEVELS[0];
  if (!entry) throw new Error('Реестр уровней пуст.');
  return validateLevel(entry.raw, ITEM_DEFS);
}

export function nextLevelId(current: string): string | null {
  const index = LEVELS.findIndex((level) => level.id === current);
  if (index === -1) return null;
  return LEVELS[index + 1]?.id ?? null;
}
```

- [ ] **Step 4: Запустить тест и убедиться, что проходит**

Run: `npm test src/levels/registry.test.ts`
Expected: PASS, четыре теста.

- [ ] **Step 5: Прогнать весь набор**

Run: `npm test`
Expected: PASS, 109 тестов. Старый вызов `loadLevel()` без аргумента в
`src/main.ts:47` и в `src/core/walkthrough.test.ts:11` продолжает работать —
аргумент необязательный.

- [ ] **Step 6: Коммит**

```bash
git add src/levels/index.ts src/levels/registry.test.ts
git commit -m "Keep the order of levels in the registry, not in the level data"
```

---

### Task 3: Сквозной проход второго уровня

**Files:**
- Test: `src/core/walkthrough-02.test.ts`

**Interfaces:**
- Consumes: `loadLevel('level_02')` из задачи 2; класс `World` из
  `src/core/world.ts` с методами `describe(id)`, `interact(id)`, `inventory()`,
  `held()`, `setHeld(id)`, `isDestroyed(id)`, `isDoorOpen(id)`,
  `checkTriggers(x, z)`, свойством `won` и подпиской `on(cb)`.
- Produces: ничего для следующих задач; это проверка.

- [ ] **Step 1: Написать тест**

Создать `src/core/walkthrough-02.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { loadLevel } from '../levels';
import { World } from './world';

/**
 * Второй уровень целиком, теми же вызовами, что делает игровой цикл.
 * Ломается вместе с реестром взаимодействий — как и тест первого уровня.
 */
function fresh(): World {
  const loaded = loadLevel('level_02');
  if (!loaded.ok) throw new Error(loaded.errors.join('\n'));
  return new World(loaded.level);
}

describe('уровень 2 проходится целиком', () => {
  it('архив -> железный ключ -> решётка -> стальной ключ -> EXIT', () => {
    const world = fresh();
    const said: string[] = [];
    world.on((event) => { if (event.kind === 'said') said.push(event.text); });

    // Обе запертые двери заперты с самого начала.
    expect(world.describe('d_corr_elec').ok).toBe(false);
    expect(world.describe('d_corr_exit').ok).toBe(false);

    // Железный ключ лежит в архиве и подбирается в инвентарь.
    expect(world.interact('key_iron').ok).toBe(true);
    expect(world.inventory()).toContain('key_iron');

    // Он открывает решётку щитовой и расходуется.
    world.setHeld('key_iron');
    expect(world.interact('lock_grate').ok).toBe(true);
    expect(world.isDestroyed('lock_grate')).toBe(true);
    expect(world.held()).toBe(null);

    // Дверь в щитовую открылась, за ней стальной ключ.
    expect(world.describe('d_corr_elec').ok).toBe(true);
    expect(world.interact('key_steel').ok).toBe(true);

    // Второй ключ снимает замок с двери EXIT.
    world.setHeld('key_steel');
    expect(world.interact('lock_exit').ok).toBe(true);
    expect(world.isDestroyed('lock_exit')).toBe(true);

    expect(said).toEqual([
      'Решётка звякнула и упала на бетон.',
      'Замок поддался. Дверь свободна.',
    ]);

    // Дверь открывается, но победа наступает только в конце коридора.
    expect(world.describe('d_corr_exit').ok).toBe(true);
    world.interact('d_corr_exit');
    expect(world.isDoorOpen('d_corr_exit')).toBe(true);
    expect(world.won).toBe(false);
    world.checkTriggers(19.5, 23.5);
    expect(world.won).toBe(true);
  });

  /**
   * Обрезок трубы существует ровно ради этого: он учит игрока, что не всякий
   * подобранный предмет — ключ, и это первый уровень, где отказ
   * «сюда не подходит» вообще может случиться.
   */
  it('обрезок трубы не открывает ничего', () => {
    const world = fresh();
    world.interact('pipe');
    world.setHeld('pipe');

    expect(world.interact('lock_grate').ok).toBe(false);
    expect(world.isDestroyed('lock_grate')).toBe(false);
    expect(world.interact('lock_exit').ok).toBe(false);
    expect(world.isDestroyed('lock_exit')).toBe(false);
    // Труба на месте: неудачная попытка ничего не расходует.
    expect(world.held()).toBe('pipe');
  });

  it('стальной ключ не открывает решётку, а железный — дверь EXIT', () => {
    const world = fresh();
    world.interact('key_iron');
    world.setHeld('key_iron');
    expect(world.interact('lock_exit').ok).toBe(false);
    expect(world.isDestroyed('lock_exit')).toBe(false);
  });
});
```

- [ ] **Step 2: Запустить**

Run: `npm test src/core/walkthrough-02.test.ts`
Expected: PASS, три теста.

Тест написан по данным из задачи 1 и падать не должен. Если падает — виноваты
данные или реестр, а не тест: сверить идентификаторы и тексты `say` с
`level_02.json`, **тексты в тесте не подгонять под данные вслепую.**

- [ ] **Step 3: Прогнать весь набор**

Run: `npm test`
Expected: PASS, 112 тестов.

- [ ] **Step 4: Коммит**

```bash
git add src/core/walkthrough-02.test.ts
git commit -m "Walk the second level end to end in a test"
```

---

### Task 4: Кнопка «Дальше» на экране победы

**Files:**
- Modify: `index.html` (стили в `<style>`, разметка внутри `#win`)
- Modify: `src/main.ts` (строка 47 и блок обработчика победы около строки 101)

**Interfaces:**
- Consumes: `loadLevel(id?)` и `nextLevelId(current)` из задачи 2.
- Produces: ничего для следующих задач.

**Почему кнопка не требует возни с вводом:** к моменту победы захват курсора уже
отпущен, игровой цикл остановлен, экранное управление спрятано
(`src/main.ts:103` и ниже). Кнопка внутри `#win` кликается и мышью, и пальцем.

- [ ] **Step 1: Добавить стиль кнопки**

В `index.html`, в блок `<style>`, сразу после правил `#win .again`:

```css
      #win button { font: 600 20px system-ui, sans-serif; color: #1a1a1a;
                    background: rgba(0,0,0,0.10); border: 2px solid #1a1a1a;
                    border-radius: 10px; padding: 12px 28px; min-height: 48px;
                    cursor: pointer; }
      #win button[hidden] { display: none; }
```

`min-height: 48px` — тот же размер под палец, что у экранных кнопок.

- [ ] **Step 2: Добавить разметку**

В `index.html` заменить блок `#win` целиком:

```html
    <div id="win" hidden>
      <p>Ты выбрался.</p>
      <button id="win-next" type="button" hidden>Дальше</button>
      <p class="again" id="win-again">Обнови страницу, чтобы пройти заново.</p>
    </div>
```

- [ ] **Step 3: Читать уровень из адреса**

В `src/main.ts` заменить строку 47:

```ts
const loaded = loadLevel();
```

на:

```ts
// Хеш разбирается здесь, а не в реестре: реестр обязан запускаться в тестах,
// где нет ни `location`, ни `window`.
const loaded = loadLevel(location.hash.slice(1));
```

И дополнить импорт в строке 7:

```ts
import { loadLevel, nextLevelId } from './levels';
```

- [ ] **Step 4: Показать кнопку при победе**

В `src/main.ts` после строки `if (!flashEl || !winEl) throw new Error('Разметка финала не найдена.');`
добавить:

```ts
const nextButton = document.querySelector<HTMLButtonElement>('#win-next');
const againEl = document.querySelector<HTMLElement>('#win-again');
const nextId = nextLevelId(level.id);
if (nextButton && nextId !== null) {
  nextButton.addEventListener('click', () => {
    location.hash = nextId;
    // Перезагрузка переиспользует весь проверенный путь загрузки целиком.
    location.reload();
  });
}
```

Внутри обработчика события `won`, сразу после `winEl.hidden = false;`:

```ts
    if (nextButton && nextId !== null) {
      nextButton.hidden = false;
      // «Обнови страницу, чтобы пройти заново» относится к последнему уровню.
      // Рядом с кнопкой «Дальше» это два противоречащих совета.
      if (againEl) againEl.hidden = true;
    }
```

- [ ] **Step 5: Проверить типы и тесты**

Run: `npm test`
Expected: PASS, 112 тестов, `tsc` без ошибок.

- [ ] **Step 6: Проверить глазами**

Машинной проверки у этого шага нет и быть не может: под headless нет ни WebGL,
ни Pointer Lock. Проверяет человек.

Run: `npm run dev`

1. Открыть `http://localhost:5173/bandy/#level_02` — грузится подвал, старт в
   котельной лицом к двери.
2. Пройти второй уровень целиком: архив → железный ключ → решётка → стальной
   ключ → EXIT → коридор → белый торец → победа.
3. Убедиться, что на экране победы второго уровня кнопки «Дальше» **нет**, а
   строка «Обнови страницу» есть.
4. Открыть `http://localhost:5173/bandy/` без хеша — грузится первый уровень.
5. Пройти первый уровень, нажать «Дальше» — адрес получает `#level_02`, страница
   перезагружается, начинается подвал.

Смотреть при этом: не косая ли геометрия, читается ли развилка в главном
коридоре, светится ли торец коридора в конце, а не сбоку.

- [ ] **Step 7: Коммит**

```bash
git add index.html src/main.ts
git commit -m "Offer the next level from the win screen"
```

---

### Task 5: Записать находку и обновить документы

**Files:**
- Modify: `known-issues.md`
- Modify: `CLAUDE.md`

**Interfaces:** ничего не потребляет и не производит; это запись.

- [ ] **Step 1: Записать протечку рендера в известные проблемы**

В `known-issues.md`, в раздел «2. Осознанно отложенное по итогам ревью», добавить
пункт первым в списке:

```markdown
- **Ориентация финального коридора зашита в рендере, а формат о ней молчит.**
  `buildExitGlow()` в `src/render/sign.ts` строит белый торец как плоскость
  шириной `x1 - x0` у стены `z1` комнаты с триггером победы. Коридор, идущий
  вдоль +x, получит белую стену во всю длинную сторону вместо торца — и при этом
  **пройдёт валидацию**: `validateLevel` про это требование не знает. Автор карты
  узнаёт об ошибке только глазами. Найдено при проектировании второго уровня,
  первая версия его карты была нарисована именно так. Чинится либо проверкой в
  валидаторе (коридор победы обязан быть длиннее вдоль z, чем вдоль x), либо
  выводом ориентации из геометрии в `buildExitGlow`. Второе лучше: оно снимает
  ограничение, а не сторожит его.
```

- [ ] **Step 2: Обновить CLAUDE.md**

В `CLAUDE.md`, в раздел «Формат уровня», добавить абзац в конец:

```markdown
Уровней два: `level_01` (здание) и `level_02` (подвал). Порядок прохождения живёт
в реестре `LEVELS` внутри `src/levels/index.ts`, а не в данных уровня: валидатор
видит уровень в одиночку и поле `next` внутри JSON проверить не смог бы. Текущий
уровень берётся из `location.hash`; переход на следующий — кнопка «Дальше» на
экране победы, которая ставит хеш и перезагружает страницу, переиспользуя весь
существующий путь загрузки вместо разбора `main.ts` на переиспользуемую функцию.

**Финальный коридор обязан идти вдоль +z**, белым торцом в сторону больших z:
это требование `buildExitGlow()`, и валидатор его не проверяет. Подробности —
в `known-issues.md`.
```

- [ ] **Step 3: Коммит**

```bash
git add known-issues.md CLAUDE.md
git commit -m "Write down that the exit corridor may only run one way"
```

---

## Порядок задач и что после каждой видно

| Задача | Что появляется |
|---|---|
| 1 | Данные второго уровня существуют и проходят валидацию; ловушка «ключ за своей же дверью» проверена |
| 2 | Уровень можно загрузить по идентификатору, игра знает порядок прохождения |
| 3 | Второй уровень доказано проходим машинно |
| 4 | В него можно играть и попадать с первого |
| 5 | Найденная протечка записана там, где её найдёт следующий читатель |
